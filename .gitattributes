# Checkout with CRLF and every bash script dies on `\r` (playbook §9).
* text=auto eol=lf

# Never touched: binary by nature, and a line-ending rewrite corrupts them.
*.png   binary
*.ico   binary
*.icns  binary
*.jpg   binary
*.jpeg  binary
*.webp  binary
*.woff  binary
*.woff2 binary
*.ttf   binary
*.otf   binary
*.clapp binary
